const users = ["Mango", "Poly", "Ajax", "Chelsey"];
users.push("Lux");
users.push("Tuce");


// users.map((item) => {
//     console.log(item)
// })

// const result = users.find((item) => item === "Tuce"); // uyuyorsa bulur
// console.log(result)

// const filtered = users.filter((item) => item.includes("u")); // filtreleme yapar
// console.log(filtered)

// const some = users.some((item) => item.includes("u")) // böyle bir şey varsa true döner yoksa false döner.
// console.log(some)

// const every = users.every((item) => item.includes("a")); // tüm elemanlar şarta uyarsa true
// console.log(every)
